set(UnicyclePlanner_VERSION @UnicyclePlanner_VERSION@)

find_package(iDynTree 0.9.2 REQUIRED)

find_package(Eigen3 REQUIRED)


if(NOT TARGET UnicyclePlanner)
  include("${CMAKE_CURRENT_LIST_DIR}/unicycle_planner.cmake")
endif()
